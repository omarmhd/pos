# تقرير التدقيق المحاسبي والبرمجي النهائي — نظام «الميزان»

**التاريخ:** 2026-06-17 | **النطاق:** محرّك القيود + القوائم المالية + سيناريو شركة متعددة الفروع
**المنهج:** تدقيق الكود المصدري + مطابقة نتائج اختبارك الحيّ على `pos.codetale.tech`

---

## 1. الحكم العام

المحرّك المحاسبي **سليم في جوهره**: كل قيد يومية متوازن (مدين=دائن يُفرض برمجياً في `buildEntry` سطر 134)، ميزان المراجعة متوازن، الميزانية متوازنة رياضياً، وتكلفة البضاعة تُحتسب تلقائياً لحظة البيع. لكن يوجد **5 أخطاء برمجية حقيقية** + **مسائل بيانات/تهيئة** تمنع الاعتماد التشغيلي قبل معالجتها.

---

## 2. الأخطاء البرمجية المؤكدة

### ✅ (1) عرض الأصول المتداولة السالبة — أُصلح
`balance_sheet.blade.php`: كانت الأصول الموجبة فقط تُعرض بينما الإجمالي يشمل السالبة، فلا تتطابق السطور مع الإجمالي (سبب فجوة 42,488 مقابل −13,857). أضفتُ صف «(خصم)» للأصول المتداولة السالبة فصارت تتطابق.

### ✅ (2) تنسيق التاريخ في أوامر الشراء — أُصلح
عمود «التسليم المتوقع» كان يعرض `2026-06-03T00:00:00.000000Z`. أضفتُ `editColumn('expected_delivery_date')` بصيغة `Y-m-d`.

### 🔴 (3) ازدواج حساب ضريبة المدخلات + الإعدادات تحرّر المفتاح الخطأ — عالي
- الترحيل (`postPurchase:489`, `postExpenseInvoice:894`) يستخدم مفتاح `account_tax_input_code` ← الحساب **1260**.
- شاشة الإعدادات (`SettingController:39`) تتحقق وتحرّر مفتاحاً مختلفاً `account_input_vat_code` ← الحساب **1150**.
- يوجد حسابان لضريبة المدخلات: **1150** (دليل الحسابات) و**1260** (migration). الحساب 1150 يبقى يتيماً، وكل الضريبة في 1260.
- الحساب 1260 أُنشئ **بدون `sub_type`** (migration سطر 27).
- **الأثر:** تعديل المدير لحساب الضريبة من الإعدادات لا يؤثر على الترحيل الفعلي → خطر في إقرار ض.ق.م.
- **الإصلاح المقترح:** توحيد المفتاح (إمّا تغيير الترحيل ليقرأ `account_input_vat_code`، أو جعل شاشة الإعدادات تحرّر `account_tax_input_code`)، وإعطاء 1260 قيمة `sub_type='current_asset'`، ودمج/تعطيل الحساب المكرّر.

### 🔴 (4) إيراد الخدمات يُرحَّل افتراضياً لحساب «مردودات المبيعات» — عالي
- `postServiceInvoice:944`: `account_service_revenue_code` افتراضي **4200** = «مردودات المبيعات» (حساب عكسي للإيراد!).
- `ServiceInvoiceController:40`: نفس الافتراض، وحقل `service_account_id` قابل للترك فارغاً.
- **الأثر:** أي فاتورة خدمات بلا حساب صريح تُسجَّل إيراداتها في حساب المردودات → تشويه الإيراد وض.ق.م المخرجات.
- **الإصلاح المقترح:** إنشاء حساب إيراد خدمات مستقل (مثل 4400) وضبط الإعداد عليه، ومنع حفظ فاتورة خدمات بلا حساب صحيح.

### 🔴 (5) تشظّي حساب الصندوق بين الفروع — عالي (جذر مشكلة الصندوق السالب)
- مبيعات POS ← `cashOrBankId(branch)` ← **1000.01** (صندوق الفرع).
- فاتورة خدمات نقدية ← `account_cash_code` ← **1000** (الحساب الأب).
- أي تدفق بلا فرع ← `fallbackCashId` ← **1000.00**.
- **الأثر:** النقدية تنقسم على 3 حسابات؛ في بياناتك ظهر 1000 بـ (66,369.25) و1000.01 بـ (1,880.00) — وهذا بالضبط ما رصدتَه. لا يمكن قراءة رصيد نقدية موحّد للفرع.
- **الإصلاح المقترح:** توحيد منطق اختيار حساب الصندوق بحيث يستخدم كل تدفق نقدي لفرعٍ **نفس حساب الفرع** (1000.XX)، وعدم السقوط للأب 1000 أو 1000.00 إلا للشركة أحادية الفرع.

---

## 3. مسائل بيانات/تهيئة (ليست أخطاء كود)

| الملاحظة | الحقيقة | الإجراء |
|---|---|---|
| الصندوق سالب إجمالاً | مدفوعات نقدية للمشتريات (91,594) > تحصيلات (25,224)، وبلا رأس مال افتتاحي | إدخال **الأرصدة الافتتاحية + رأس المال** قبل التشغيل |
| حقوق الملكية = 0 | حساب 3000 لم يُموَّل | إدخال رأس المال من شاشة الأرصدة الافتتاحية |
| عجز وردية على 6520 بدل 6530 | الكود الحالي يستخدم **6530 الصحيح** (سطر 1232) | قيد قديم/إعداد مُتجاوَز — راجع قيمة `account_pos_cash_shortage_code` وأعد تصنيف القيد القديم |
| ض.ق.م مدخلات 1150 دائن بـ 2.00 | سند قبض RV-20260613-0001 رُحّل على 1150 | مراجعة السند يدوياً |
| فرق 2 ج.م في التدفقات النقدية | نفس السند أعلاه | يزول بتصحيح السند |
| نص «فرخل» في حالة سند | غير موجود في الكود إطلاقاً → قيمة مخزّنة خاطئة | تحديد السند وتصحيح قيمته |
| Vite 503 | أداة تطوير فقط | `npm run build` للإنتاج |

---

## 4. رأيي كشركة متعددة الفروع تستخدم النظام

النظام **جاهز محاسبياً من حيث المبدأ** (توازن، COGS تلقائي، قوائم وفق IFRS، مرتجعات عاكسة). لكن **لن أعتمده في فروع متعددة قبل** معالجة ثلاثة أمور:

1. **توحيد حساب الصندوق لكل فرع** (البند 5) — وإلا فكل تقرير نقدية للفرع غير موثوق.
2. **تصحيح مسار ضريبة المدخلات** (البند 3) — وإلا فإقرار الضريبة معرّض للخطأ.
3. **تصحيح حساب إيراد الخدمات** (البند 4) — وإلا فالإيراد مُشوَّه.

وعلى مستوى التشغيل: **إلزام إدخال الأرصدة الافتتاحية ورأس المال** كخطوة go-live إجبارية، لأن غيابها هو ما يجعل الصندوق يبدو سالباً.

**التقييم:** دقة رياضية 100% ✅ | سلامة التوازن ✅ | التصنيف المحاسبي ⚠️ (بنود 3،4،5) | اكتمال البيانات الافتتاحية ❌ | الجاهزية للفروع ⚠️ بعد الإصلاحات.

---

## 5. ما أُصلح فعلاً في هذه الجلسة
- عرض الأصول المتداولة السالبة في الميزانية (`balance_sheet.blade.php`).
- تنسيق تاريخ التسليم المتوقع في أوامر الشراء (`PurchaseOrderController`).

> البنود 3 و4 و5 تمسّ ربط حسابات وبيانات تاريخية وإقرارات ضريبية، فتركتُها كتوصيات بإصلاحات محددة لتعتمد اتجاهها قبل التنفيذ.
